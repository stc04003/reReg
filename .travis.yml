# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
sudo: false
cache: packages
sudo: false
r:
  - oldrel
  - release
  - devel
before_install:
  - tlmgr install index
pandoc_version: 2.2.3.2
  